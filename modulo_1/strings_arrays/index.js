/**const seuNome = prompt ('Qual é seu nome?')
const seuEmail = prompt ('Qual é seu E-mail')

const frase = "Olá seja bem-vinda(O) $[seuNome], O E-mail $[seuEmail] foi cadrastado com sucesso."

console.log(frase)

const comidas = [ameixa,uva,melão,caqui,manga]
const phrase = "Essas são as minhas comidas favoritas ${comidas}"
const comidaFavorita = prompt ("Qual é sua comida favorita?")
console.log(comidas.posh(comidaFavorita)**/

function minhaFuncao(variavel) {
	return variavel * 5
}

(minhaFuncao(2)
(minhaFuncao(10)