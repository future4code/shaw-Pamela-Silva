/**Exercícios de interpretação de código**/
/**1-A) Na primeira linha será impresso o nome do ator Matheus Nachtergaele.
 Na segunda linha será impresso a quantidade de caracteres do nome de Selton Mello.
 Na terceira linha será impresso a emissora e o horario que passará na tv. **/

 /**2-A) Os nomes serão atualizados o cachorro serán "Juca" o gato será "Juba" e a tartaruga será "Jubo" preservando a idade e a raça dos animais.
B) A sintaxe seria os as constantes com os nomes dos animais.**/

/**3-A) Será impresso só o boleano false.
 B) Chegaras ãos reseultados porque "backender" retorna a false e a string "altura" não existe e não esta sendo adicionada**/

 /**Exercícios de escrita de código**/
/**1-A)**/
const pessoa = {
  nome:"Edwania"
  apelidos:["vania", "vaninha", "ed"] 

console.log("Olá me chamo" pessoa.nome "más pode me chamar de" pessoa.apelido[0],pessoa.apelido[1], "ou de" pessoa.apelido [2].)
}
/**B)**/
const novaPessoa = {
  nome:"Luiza"
  apelido:["LU","Luz","Iza"]
  console.log("Olá me chamo" pessoa.nome "más pode me chamar de" pessoa.apelido[0],pessoa.apelido[1], "ou de" pessoa.apelido [2].)
}
/**2-A)**/
const humano = {
    nome:"Marlo"
    idade:42,
    profissao:"Engeneiro civil"
}
const humano1 = {
    nome1:"Eliza"
    idade1:35,
    profissao1:"Juiza"
}

const humam = (Marlon)  => {
 const resultado = [nome.length, idade,profissao]
 return resultado
}
console.log(resultado(humano))

const humam1 = (Eliza) => {
    const resultado1 = [nome1.length,idade1,profissao1]
return resultado1
}
console.log(resultado(
} 

const nome =prompt("Qual é seu nome?")
const genero =prompt("Qual e seu gênero?")
genero = if (genero == "feminino") {
  mensagem = "Bem-vinda!";
} else {genero == "masculino"
  mensagem = "Bem-vindo!";